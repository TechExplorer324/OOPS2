/**
 * Interface (VI, VIII) for objects that can log messages.
 * Used to demonstrate multiple interface implementation.
 */
interface Loggable {
    /**
     * Logs a message.
     * @param message The message to log.
     */
    void log(String message);
}
